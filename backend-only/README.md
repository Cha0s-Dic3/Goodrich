# Goodrich Backend Only

## Run locally

1. Copy `.env.example` to `.env`
2. Set `MONGODB_URI` and `JWT_SECRET`
3. Install dependencies:

```bash
npm install
```

4. Start server:

```bash
npm run dev
```

Server runs on `http://localhost:5174` by default.
